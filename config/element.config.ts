import type { NuxtConfig } from "nuxt/schema";

// Configuração do módulo do ElementPlus um framework de CSS para Vue
const elementConfig: NuxtConfig["elementPlus"] = {
  defaultLocale: "pt-br",
  themes: ["dark"],
};

export default elementConfig;
