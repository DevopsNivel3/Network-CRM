import type { NuxtConfig } from "nuxt/schema";

// Configuração do módulo Prisma ORM, usado para comunicação entre backend/database
const prismaConfig: any = {
  installStudio: false,
};

export default prismaConfig;
